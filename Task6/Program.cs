// ============================================================
// ЗАДАЧА 6: Угадай число — выносим валидацию в метод
// Что учим: методы, параметры, out, возврат bool
// Связь с задачей 1
// ============================================================
// Возьми задачу 1 и вынеси проверку ввода в отдельный метод.
// Метод принимает строку, через out отдаёт распарсенное число,
// возвращает bool — валидно ли.
// В основном коде цикл становится чище — только вызов метода и логика игры.
 
var randomNumber2 = Random.Shared.Next(1, 11);
 
// TODO: напиши метод bool TryReadNumber(string input, out int number)
// внутри: TryParse + проверка диапазона 1–10
// если всё ок — number = значение, return true
// иначе — number = 0, return false
 
// TODO: перепиши цикл из задачи 1, используя TryReadNumber

Console.WriteLine("Задача 6.");